{% extends 'base.html.twig' %}

{% block body %}
    <div id="container-fluid">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#"><img class="plane" src="{{ asset('build/images/plane.ec239e52.jpeg') }}"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('planemodel_index') }}">Planes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('reservation_index') }}">Bookings</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('flight_index') }}">Flights</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('site_index') }}">Airports</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('review_index') }}">Reviews</a>
                    </li>
                </ul>
            </div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">

                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('fos_user_security_login') }}">Sign in</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('fos_user_registration_register') }}">Sign out</a>
                    </li>
                </ul>
            </div>
        </nav>
        <h1 class="text-center">Welcome on Flyaround</h1>
    </div>
{% endblock %}

