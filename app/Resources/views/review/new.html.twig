{% extends 'base.html.twig' %}

{% block body %}
    <h1>New review</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('review_index') }}">Back to review</a>
        </li>
    </ul>
{% endblock %}
